# Smart Factory IoT - Development Environment Configuration
# This file contains environment variables for local development
# Copy this to .env when setting up development environment

# Environment Mode
NODE_ENV=development

# Database Configuration
# Format: mysql://[user[:password]@]host[:port]/database
DATABASE_URL=mysql://root@localhost:3306/smart_factory_dev

# Security
# For development, this is a simple key. Use a strong key in production!
JWT_SECRET=dev-secret-key-change-in-production

# Server Configuration
PORT=3000

# Application Configuration
VITE_APP_ID=smart-factory-iot-dev
VITE_APP_TITLE=Smart Factory IoT (Development)

# CORS Configuration
# Allow all origins in development for easier testing
CORS_ORIGINS=*

# Debug Mode
# Set to true to enable verbose logging
DEBUG=true

# Optional: Google Maps API Key
# Get your key from: https://console.cloud.google.com/
VITE_GOOGLE_MAPS_API_KEY=dev-key-optional

# Optional: OAuth Server URL (for local development)
# Uncomment if using local OAuth server
# OAUTH_SERVER_URL=http://localhost:8080
# VITE_OAUTH_PORTAL_URL=http://localhost:8080
